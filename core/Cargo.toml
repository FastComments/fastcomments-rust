[package]
name = "fastcomments-core"
version = "0.0.1"
authors = ["Fastcomments"]
description = "A Rust library for FastComments"
license = "MIT"
edition = "2021"

[dependencies]
serde = { version = "^1.0", features = ["derive"] }
serde_json = "^1.0"